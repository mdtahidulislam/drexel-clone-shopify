<!DOCTYPE html>
<html lang="{{ request.locale.iso_code }}">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <!-- canonical url -->
    <link rel="canonical" href="{{ canonical_url }}">

    <!-- add favicon -->

    <!-- add css -->
    {{ 'bootstrap-v5.0.2.min.css' | asset_url | stylesheet_tag }}
    {{ 'fontawesome-v6.min.css' | asset_url | stylesheet_tag }}
    {{ 'owl.carousel-v2.3.4.min.css' | asset_url | stylesheet_tag }}
    {{ 'owl.theme.default-v2.3.4.min.css' | asset_url | stylesheet_tag }}
    {{ 'style.css' | asset_url | stylesheet_tag }}
    
    <!-- page title -->
    <title>{{ page_title }}</title>

    <!-- load scripts from shopify -->
    {{ content_for_header }}
</head>
<body>
    <!-- main content -->
    
    <main>
        <!-- dynamically output from all template  -->
        {{ content_for_layout }}
    </main>

    <!-- all js -->
    <script src="{{ 'jquery-v3.6.0.min.js' | asset_url }}" defer></script>
    <script src="{{ 'popper-v2.9.2.min.js' | asset_url }}" defer></script>
    <script src="{{ 'bootstrap-v5.0.2.min.js' | asset_url }}" defer></script>
    <script src="{{ 'main.js' | asset_url }}" defer></script>
</body>
</html>